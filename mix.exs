defmodule Benchfella.Mixfile do
  use Mix.Project

  def project do
    [app: :benchfella,
     version: "0.2.0-dev",
     elixir: "~> 1.0"]
  end

  def application do
    [applications: []]
  end

  # no deps
  # --alco
end
